[build]
  base = "frontend"
  command = "npm install && npm run build -- --configuration production"
  publish = "dist/perutalent-frontend"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
