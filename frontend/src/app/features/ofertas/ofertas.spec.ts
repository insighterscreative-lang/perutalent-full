import { describe, expect, it } from 'vitest';
import { OfertasComponent } from './ofertas';

describe('OfertasComponent', () => {
  it('está disponible para el enrutamiento', () => {
    expect(OfertasComponent).toBeDefined();
  });
});
